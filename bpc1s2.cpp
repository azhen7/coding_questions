#include <iostream>
#include <string>
#include <algorithm>
#include <vector>

typedef long long LL;

std::vector<LL> stationPositions;
LL N, Q, C;

int main()
{
    std::cin.sync_with_stdio(false);
    std::cin.tie(0);

    std::cin >> N >> C >> Q;

    stationPositions.reserve(N);

    LL l = 0;
    LL r = C;

    
}